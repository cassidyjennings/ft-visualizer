import type { Settings } from "./types";

export const defaultSettings: Settings = {
  coloring: "system",
  center: "centerPixel",
  shift: "shifted",
  magScale: "linear",
  magNormalize: "max",
  normalization: "forward",
  axis1DOrigin: "left",
};
